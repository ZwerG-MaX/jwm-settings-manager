set precompiler defines in cmake project
(specifically LINUX_COMMON_HAS_X=true) when Xlibs are found

If CheckIncludes Fails... create the Element

missing functions:

panel:

deleteTrayButton?

menu:
Configure Dynamic
