set precompiler defines in cmake project
(specifically LINUX_COMMON_HAS_X=true) when Xlibs are found

missing functions:

jwmrc:
getShortcuts

panel:
add_new_menu
deleteShortcut?
deleteTrayButton?

needs fixing:
removeAutostart - xdg stuff

node adding functions do not return the correct node... find out why???

MENU:
void getMenus(Fl_Browser* o);
std::string getLabel(Fl_Output* labler, std::string MENU);
void listMenus(Fl_Browser* o);
void addRoot(std::string menu);
void setRootMenuAttribute(std::string menustring ,std::string attribute,std::string value);
