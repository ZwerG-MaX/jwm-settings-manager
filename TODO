If CheckIncludes Fails... create the Element

NEXT features:
keyboard
GET
setxkbmap -query
SET
setxkbmap -model $model -layout $layout -variant $options
GET
localectl status
SET
localectl set-x11-keymap layout $model $variant $options

menu:
Configure Dynamic
Desktop names
windows->decorations/outline decorations
ShutdownCommand
RestartCommand
PopupStyle
    delay (int)
    enabled (true,false,button,clocks,menu,pager,task)
  Font
  Outline
  Foreground
  Background
  
